# Reports Directory

This directory contains generated outputs from experiments:

- Plots (solution fields, error maps, convergence curves)
- Tables (error metrics, hyperparameter sensitivity)
- Logs (training progress)

All files here are gitignored.
